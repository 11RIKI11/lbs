html
  head
    title= 'Lab9'
    style
      include style.css
  body
    h2= header
    if msg
      p= msg
    if rows.length
      table
        tr
          th= 'id'
          th= 'title'
          th= 'short_code'
          for rec in rows
            tr
              td= rec.id
              td= rec.title
              td= rec.short_code
